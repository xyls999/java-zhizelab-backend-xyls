
spring:
  active: dev
  datasource:
    url: jdbc:mysql://localhost:3306/sm_db?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: lhy145632789
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 如果你要用 druid 连接池（可选）
    type: com.alibaba.druid.pool.DruidDataSource

mybatis-plus:
  type-aliases-package: com.atguigu.pojo
  mapper-locations: classpath:/mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-field: isDeleted
      table-prefix: news_
      id-type: auto

jwt:
  token:
    tokenExpiration: 120
    tokenSignKey: headline123456

app:
  upload-dir: uploads

llm:
  # OpenAI-compatible endpoint, e.g. https://api.openai.com/v1/chat/completions
  base-url: ""
  api-key: ""
  model: gpt-4o-mini
  temperature: 0.7
